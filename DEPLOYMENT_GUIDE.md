# دليل النشر على Vercel - بيت الريف

## 📋 المتطلبات
- ✅ حساب Vercel Pro (متوفر)
- ✅ حساب GitHub (متوفر)
- ✅ النطاقات: bietalreef.ae و www.bietalreef.ae (مُعدة)

## 🚀 خطوات النشر

### الطريقة 1: النشر من لوحة تحكم Vercel (موصى بها)

#### 1. إنشاء مستودع GitHub جديد
```bash
cd /home/ubuntu/bietalreef-v2
git init
git add .
git commit -m "Initial commit: Biet Alreef Super App v2"
gh repo create bietalreef-v2 --public --source=. --remote=origin --push
```

#### 2. الدخول إلى Vercel Dashboard
- افتح: https://vercel.com/dashboard
- سجل الدخول بحساب GitHub

#### 3. استيراد المشروع
1. انقر على **"Add New..."** → **"Project"**
2. اختر **"Import Git Repository"**
3. ابحث عن `bietalreef-v2`
4. انقر **"Import"**

#### 4. إعدادات المشروع
```
Framework Preset: Other
Build Command: pnpm build
Output Directory: client/dist
Install Command: pnpm install
Root Directory: ./
```

#### 5. متغيرات البيئة (Environment Variables)
سيتم نقلها تلقائياً من Manus، لكن تأكد من:
- `DATABASE_URL`
- `JWT_SECRET`
- `VITE_APP_TITLE`: بيت الريف - منصة البناء الذكية
- `VITE_APP_LOGO`: /logo.png

#### 6. النشر
- انقر **"Deploy"**
- انتظر 2-3 دقائق

#### 7. ربط النطاقات
1. بعد النشر، اذهب إلى **Settings** → **Domains**
2. أضف النطاقات:
   - `bietalreef.ae`
   - `www.bietalreef.ae`
3. اتبع التعليمات لتحديث DNS

---

### الطريقة 2: النشر باستخدام Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
cd /home/ubuntu/bietalreef-v2
vercel --prod

# ربط النطاقات
vercel domains add bietalreef.ae
vercel domains add www.bietalreef.ae
```

---

## 🔄 التحديثات التلقائية

بعد الربط بـ GitHub، أي تحديث على الفرع `main` سيتم نشره تلقائياً:

```bash
# تعديل الملفات
git add .
git commit -m "Update: وصف التحديث"
git push origin main
```

---

## ✅ التحقق من النشر

1. **الموقع الرئيسي:**
   - https://bietalreef.ae
   - https://www.bietalreef.ae

2. **فحص الأيقونات:**
   - افتح المتصفح → أدوات المطور → Network → ابحث عن `favicon.ico`
   - يجب أن يظهر شعار بيت الريف

3. **فحص PWA:**
   - افتح الموقع على الهاتف
   - يجب أن يظهر زر "تثبيت التطبيق"

4. **فحص SSL:**
   - تأكد من ظهور القفل الأخضر في شريط العنوان

---

## 🎯 الخطوات التالية

### المرحلة 2: ربط مساعد وياك الذكي
- [ ] إضافة Gemini API للواجهة العامة
- [ ] إضافة OpenAI API للملف الشخصي
- [ ] إنشاء واجهة الدردشة

### المرحلة 3: قاعدة البيانات والمصادقة
- [ ] إعداد Supabase
- [ ] تفعيل تسجيل الدخول
- [ ] إنشاء جداول قاعدة البيانات

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من سجلات النشر في Vercel Dashboard
2. راجع متغيرات البيئة
3. تأكد من إعدادات DNS للنطاقات

---

**تم الإعداد بواسطة Manus AI 🤖**
**© 2025 بيت الريف - جميع الحقوق محفوظة 🇦🇪**
